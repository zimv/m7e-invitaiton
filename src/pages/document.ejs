<!doctype html>
<html>
<head>
    <meta charset="utf-8" />
    <title>invites</title>
    <script>
      function init () {
        const width = document.documentElement.clientWidth;
        document.documentElement.style.fontSize = width / 375 + 'px';
      }
      init();
      window.addEventListener('orientationchange', init);window.addEventListener('resize', init);
    </script>
</head>
<body>
<div id="root"></div>
</body>
</html>
